# Orivan — environment variables
#
# Copy this file to `.env.local` and fill in any values you need.
# Only public, build-time configuration belongs here.
# Never commit secrets, API keys, or credentials.

# Public site URL used for canonical links, sitemap, and Open Graph metadata.
# Leave blank for local development.
VITE_SITE_URL=
